[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[project]
name = "sixdegrees"
version = "0.2.1"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "neo4j>=6.2.0",
]

[dependency-groups]
dev = ["poethepoet>=0.45.0", "ruff>=0.15.12"]
